# YOLO v8/v11 için Ultralytics paketi (en son sürüm)
ultralytics>=8.2.0

# Görüntü işleme için OpenCV
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Sayısal hesaplamalar için NumPy
numpy>=1.24.0

# Bilimsel hesaplamalar için SciPy (kuş-drone ayrımı için gerekli)
scipy>=1.10.0

# Derin öğrenme için PyTorch (CUDA desteği için)
torch>=2.0.0
torchvision>=0.15.0

# GUI için Tkinter (genelde Python ile gelir)
# tkinter

# Görüntü işleme için PIL/Pillow
Pillow>=9.5.0

# Kalman filtresi için filterpy
filterpy>=1.4.5

# Python için Tkinter GUI (genelde Python ile gelir ama bazen eksik olabilir)
# Linux için: sudo apt-get install python3-tk
# Windows/Mac: Genelde Python ile birlikte gelir

# Video işleme için ek paketler
imageio>=2.31.0
imageio-ffmpeg>=0.4.8

# Performans iyileştirmeleri için
numba>=0.57.0

# Loglama ve yapılandırma
pyyaml>=6.0
colorlog>=6.7.0

# Test ve geliştirme için (opsiyonel)
pytest>=7.3.0
pytest-cov>=4.1.0
black>=23.3.0
flake8>=6.0.0

# Veri görselleştirme (opsiyonel)
matplotlib>=3.7.0
seaborn>=0.12.0

# Model eğitimi için (opsiyonel)
# albumentations>=1.3.0
# tensorboard>=2.13.0